import 'package:flutter/material.dart';

class BaseSvgWidget extends StatelessWidget {
  const BaseSvgWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
